# Conversations-era stack (previously pinned to azure-ai-projects 1.x on purpose
# to stay on the Assistants-style threads/runs API - see git history for that
# rationale). agent-framework-foundry pulls in azure-ai-projects, openai and
# agent-framework-core at compatible versions; agent-framework-foundry-hosting is
# a separate package that FoundryToolbox needs for its MCP client wiring.
agent-framework-foundry==1.13.0
agent-framework-foundry-hosting
azure-identity==1.25.3
