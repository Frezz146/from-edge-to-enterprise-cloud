# eval_pipeline.py: local backend and the Part 2 MCP server it imports
# (same pins as part02-mcp-orchestration/python/requirements.txt)
foundry-local-sdk==1.2.3
mcp==1.30.0

# eval_pipeline.py: cloud backend (Azure OpenAI v1 endpoint) and the LLM judge,
# both authenticated with Microsoft Entra ID
openai>=1.108.0
azure-identity==1.25.3
azure-ai-evaluation==1.18.5

# Tracing: OTLP for a local Aspire Dashboard, Azure Monitor for Application Insights
opentelemetry-sdk>=1.30.0
opentelemetry-exporter-otlp-proto-http>=1.30.0
azure-monitor-opentelemetry>=1.6.0

# traced_agent.py imports the Part 3 agent (same pins as part03)
agent-framework-foundry==1.13.0
agent-framework-foundry-hosting
